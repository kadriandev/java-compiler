/* A program to compute the factorial value of an input */

void main(void) {
  int arr[5];
  int x; int fac;
  x = input();
  fac = 1 + arr[2];

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  }

  output(fac);
}
